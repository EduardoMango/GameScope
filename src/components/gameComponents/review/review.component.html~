<div class="register-container bg-black text-white d-flex justify-content-center align-items-center flex-column">
  <h2 class="p-3 pb-0">{{ videogame?.title }}</h2>
  <div class="form-container w-50">
    <form class="bg-dark text-white  " [formGroup]="reviewForm"(ngSubmit)="onSubmit()">
        <!-- TITULO REVIEW -->
         <h4>Review Title</h4>
       <div class="form-grup d-flex flex-column align-items-center">
        <label for="title"></label>
        <input
        type="text"
        id="title"
        formControlName="title"
        class="form-control w-75"
        placeholder="Title"
        />
        <div *ngIf="reviewForm.get('title')?.touched && reviewForm.get('title')?.invalid" class="error-msj">
          {{ titleError }}
        </div>
        <small class="error" *ngIf="reviewForm.get('title')?.errors?.['minlength']">The title must be at least 10 characters long.</small>
       </div>
       <!-- CONTENIDO -->
       <div class="form-grup d-flex flex-column align-items-center">
        <h4>General comments</h4>
        <label for="content"></label>
        <input
        type="text"
        id="content"
        formControlName="contenido"
        class="form-control w-75"
        placeholder="Write your comment..."
        />
        <div>
          <small class="error" *ngIf="reviewForm.get('content')?.errors?.['minlength']">
            Comment cannot be less than 50 Characters long
            </small>
          <small class="error" *ngIf="reviewForm.get('content')?.errors?.['maxlength']">
            The comment cannot be longer than 500 characters.
            </small>
        </div>
       </div>

       <!-- CALIFICACION -->
        <!-- Calificación con estrellas -->
        <div>
          <h4>Graphics</h4>

          <!-- Calificación con estrellas para Gráficos -->
          <p-rating
            formControlName="graphicsRating"
            [stars]="5">
          </p-rating>
          <div>
            <small class="error" *ngIf="reviewForm.get('graphicsRating')?.errors?.['required']
                                        && reviewForm.get('graphicsRating')?.touched">
              Giving a rating is required
            </small>
          </div>

          <!-- Comentario para Gráficos -->
          <textarea
            formControlName="graphicsComment"
            rows="3"
            cols="58"
            placeholder="Write your comment on the graphs...">
          </textarea>
        </div>

        <!-- Jugabilidad -->
        <div>
          <h4>Gameplay</h4>

          <!-- Calificación con estrellas para Jugabilidad -->
          <p-rating
            formControlName="gameplayRating"
            [stars]="5">
          </p-rating>
          <div>
            <small class="error" *ngIf="reviewForm.get('gameplayRating')?.errors?.['required']
                                        && reviewForm.get('gameplayRating')?.touched">
              Giving a rating is required
            </small>
          </div>


          <!-- Comentario para Jugabilidad -->
          <textarea
            formControlName="gameplayComment"
            rows="3"
            cols="58"
            placeholder=" Write your comment about the gameplay...">
          </textarea>
        </div>

        <!-- Relación Costo/Calidad -->
        <div>
          <h4> Cost/Quality Ratio</h4>

          <!-- Calificación con estrellas para Relación Costo/Calidad -->
          <p-rating
            formControlName="priceQualityRating"
            [stars]="5">
          </p-rating>
          <div>
            <small class="error" *ngIf="reviewForm.get('puntuacionPrecioCalidad')?.errors?.['required']
                                        && reviewForm.get('puntuacionPrecioCalidad')?.touched">
              Giving a rating is required
            </small>
          </div>


          <!-- Comentario para Relación Costo/Calidad -->
          <textarea
            formControlName="comentarioPrecioCalidad"
            rows="3"
            cols="58"
            placeholder=" Write your comment about the cost/quality ratio...">
          </textarea>
        </div>
        <!-- BOTON DE ENVIO -->
         <div class="button-div pt-3">
          <button type="submit" class="btn btn-primary" [disabled]="reviewForm.invalid">Submit Review</button>
        </div>

    </form>
  </div>
</div>
